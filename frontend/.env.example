# Backend API base URL.
# Local development: leave unset (defaults to http://127.0.0.1:8000).
# Production (Vercel): set this to your deployed Render backend URL, e.g.
# REACT_APP_API_URL=https://facilityiq-backend.onrender.com
REACT_APP_API_URL=
